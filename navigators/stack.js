import {createStackNavigator} from '@react-navigation/stack';
// this is so I can create a stack screen for the main.js
const Stack = createStackNavigator();
export const Screen = Stack.Screen;
export default Stack;
